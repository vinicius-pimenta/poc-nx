name: CI
on:
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: nrwl/nx-set-shas@v3
      - run: npm i

      - run: npx nx affected -t test --base=main --head=vinicius-pimenta

